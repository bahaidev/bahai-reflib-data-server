const base = location;

const idForWorkSectionAndParagraph =
  `${base}idForWorkSectionAndParagraph?work=%s&section=%s&paragraph=%s`;
const workSectionAndParagraphForId =
  `${base}workSectionAndParagraphForId?id=%s`;
const infoForUrl = `${base}infoForUrl?url=%s`;
const infoForId = `${base}infoForId?id=%s`;
const idForUrl = `${base}idForUrl?url=%s`;
const urlForId = `${base}urlForId?id=%s`;

export {
  idForWorkSectionAndParagraph,
  workSectionAndParagraphForId,
  infoForUrl,
  infoForId,
  idForUrl,
  urlForId
};
